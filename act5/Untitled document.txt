import random
import string


def generateName():
    characters = string.ascii_letters + string.digits
    while True:
        name = ''.join(random.choices(characters, k=6))
        if not photoManager.nameExists(name):
            return name